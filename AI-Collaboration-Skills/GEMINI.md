# AI Collaboration

Use the bundled AI Collaboration skills and MCP tools to preserve and resume coding-task context across agents. Prefer `ai_collaboration.get_context` when continuing existing work, and record a checkpoint when the user requests a deliberate handoff.
